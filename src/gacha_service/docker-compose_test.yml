services:
  distro:
    build: ./service
    depends_on:
      - db
    ports:
      - 5000:5000

  db:
    image: mongo:latest
    ports:
      - '27017:27017'
    command: mongod --quiet --logpath /dev/null 
    tmpfs:
      - /data/db 
