from routers.transactions.models import TransactionModel


def save_transaction(user_id: str, tux_amount: float):
    pass


def get_transactions(user_id: str, start: int, end: int) -> list[TransactionModel]:
    return []
